# RMA Source

RMA-specific OO policies and adapters live here. Supported messages and legacy
conversions are loaded from the governed parameter catalogue; they must not be
duplicated in source code.

The current Reload Test Data implementation is in [`reload-test-data/`](reload-test-data/).

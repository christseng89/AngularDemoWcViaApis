const api=process.env.SSI_BFF_URL??'http://localhost:3100/api';
async function request(path,options={}){const response=await fetch(`${api}/${path}`,{...options,headers:{'content-type':'application/json',...options.headers}});const text=await response.text();let value;try{value=JSON.parse(text);}catch{value=text;}if(!response.ok)throw new Error(`${options.method??'GET'} ${path}: ${response.status} ${JSON.stringify(value)}`);return value;}
const ssis=(await request('ssis')).filter((item)=>item.status==='ACTIVE'&&String(item.route?.sampleSet??'').match(/^(DEMO|TEN_BANK_)/));
const failures=[];const results=[];
for(const ssi of ssis){
  for(const applicability of ssi.applicability??[]){
    for(const messageType of String(ssi.route.messageTypes).split(',').map((value)=>value.trim()).filter(Boolean)){
      const route=ssi.route;const transaction={consumer:applicability.consumer,product:applicability.product,businessFunction:applicability.businessFunction,paymentLeg:applicability.paymentLeg,direction:applicability.direction,counterpartyBic:route.approvedGlobal==='true'?'BARCGB22':route.counterpartyBic,counterpartyCountry:route.approvedGlobal==='true'?'GB':route.counterpartyCountry,settlementCountry:route.settlementCountry,settlementMarket:route.settlementMarket,clearingSystem:route.clearingSystem,currency:route.currency,bookingEntity:route.bookingEntity==='ANY'?'HK01':route.bookingEntity,valueDate:'2026-09-07',amount:'1000000',messagingService:route.messagingService,messageType,transactionReference:`MATRIX-${route.ssiCode}-${messageType.replaceAll('.','-')}`};
      const label=`${route.ssiCode} | ${route.currency} | ${applicability.businessFunction} | ${route.messagingService}/${messageType}`;
      try{
        const rma=await request('rma-authorisations/check',{method:'POST',body:JSON.stringify({ownBic:route.senderBic??'DEMOHKHH',counterpartyBic:route.actualReceiverBic,service:route.messagingService,direction:'OUTBOUND',messageType,at:transaction.valueDate})});
        if(rma.authorised!==true)throw new Error(`RMA ${rma.decision}`);
        const preview=await request('resolve',{method:'POST',body:JSON.stringify(transaction)});if(!preview.recommendedRoute)throw new Error(`Preview ${preview.decision}`);
        if(![preview.recommendedRoute,...preview.alternatives].some((candidate)=>candidate.ssiId===ssi.id))throw new Error(`Expected SSI ${ssi.id} is not eligible`);
        const confirmation=await request('resolve/confirm',{method:'POST',body:JSON.stringify({attemptId:preview.attemptId,selectedSsiId:preview.recommendedRoute.ssiId,actor:'matrix.verifier'})});
        if(confirmation.decision!=='CONFIRMED'||confirmation.rmaEvidence?.decision!=='AUTHORISED'||confirmation.nostroEvidence?.decision!=='RESOLVED'||!confirmation.applicability?.businessFunction||!confirmation.effectivePeriod?.validFrom)throw new Error('Confirmation evidence incomplete');
        results.push({label,selected:preview.recommendedRoute.route.ssiCode});
      }catch(error){failures.push({label,error:error instanceof Error?error.message:String(error)});}
    }
  }
}
const exact023=ssis.find((item)=>item.route.ssiCode==='SSI-DEMO-023');const exact024=ssis.find((item)=>item.route.ssiCode==='SSI-DEMO-024');const route021=ssis.find((item)=>item.route.ssiCode==='SSI-DEMO-021');
const controls=[];
if(exact023&&exact024){const app=exact024.applicability[0];const base={consumer:app.consumer,product:app.product,businessFunction:app.businessFunction,paymentLeg:app.paymentLeg,direction:app.direction,counterpartyBic:'BARCGB22',counterpartyCountry:'GB',settlementCountry:'US',settlementMarket:'US_DOLLAR',clearingSystem:'FEDWIRE',currency:'USD',bookingEntity:'HK01',valueDate:'2026-09-07',amount:'1000000',messagingService:'FINPLUS',messageType:'pacs.009.001.12',transactionReference:'MATRIX-FALLBACK-PRECEDENCE'};const preview=await request('resolve',{method:'POST',body:JSON.stringify(base)});controls.push({control:'SSI-DEMO-023_FALLBACK_PRECEDENCE',passed:preview.recommendedRoute?.route?.ssiCode==='SSI-DEMO-024',actual:preview.recommendedRoute?.route?.ssiCode});const noRoute=await request('resolve',{method:'POST',body:JSON.stringify({...base,counterpartyBic:'ABCDZAJJ',counterpartyCountry:'ZA',settlementMarket:'UNAPPROVED'})});controls.push({control:'FAIL_CLOSED_NO_ROUTE',passed:noRoute.decision==='NO_ELIGIBLE_ROUTE',actual:noRoute.decision});}
if(route021){controls.push({control:'SSI-DEMO-021_CORRESPONDENT_ROUTE',passed:route021.route.counterpartyBic==='CHASUS33'&&route021.route.actualReceiverBic==='CITIUS33'&&route021.route.routeType==='CLEARING_AGENT',actual:`${route021.route.counterpartyBic}->${route021.route.actualReceiverBic}`});}
if(route021&&exact023){const preview=await request('resolve',{method:'POST',body:JSON.stringify({consumer:'TRADE_FINANCE',product:'IMPORT_LC',businessFunction:'IMPORT_LC_BANK_REIMBURSEMENT',paymentLeg:'BANK_REIMBURSEMENT',direction:'OUTBOUND',counterpartyBic:'CHASUS33',counterpartyCountry:'US',currency:'USD',bookingEntity:'GB01',valueDate:'2026-09-07',amount:'1000000',messageType:'pacs.009.001.12',transactionReference:'MATRIX-CHAS-IMPORT-LC'})});const codes=[preview.recommendedRoute,...preview.alternatives].filter(Boolean).map((item)=>item.route.ssiCode);controls.push({control:'CHAS_USD_IMPORT_LC_021_BEFORE_023',passed:codes.includes('SSI-DEMO-021')&&codes.includes('SSI-DEMO-023')&&preview.recommendedRoute?.route?.ssiCode==='SSI-DEMO-021',actual:codes});}
let restricted=(await request('nostro-accounts')).find((item)=>item.maskedAccountRef==='DEMO-ENTITY-RESTRICTION'&&item.status==='ACTIVE');if(!restricted){restricted=await request('nostro-accounts',{method:'POST',body:JSON.stringify({ownLegalEntityId:'US01',allowedBookingEntities:['US01'],accountServicerBic:'TESTUS33',currency:'USD',maskedAccountRef:'DEMO-ENTITY-RESTRICTION',purpose:'SETTLEMENT',priority:1,validFrom:'2026-01-01',validTo:'2027-12-31',maker:'maker.matrix',source:'SYNTHETIC_DEMO'})});for(const [action,actor] of [['submit','maker.matrix'],['approve','checker.matrix'],['activate','checker.matrix']])restricted=await request(`nostro-accounts/${restricted.id}/${action}`,{method:'POST',body:JSON.stringify({actor})});}
const denied=await request('nostro-accounts/resolve',{method:'POST',body:JSON.stringify({ownLegalEntityId:'GB01',accountServicerBic:'TESTUS33',currency:'USD',purpose:'SETTLEMENT',at:'2026-09-07'})});const allowed=await request('nostro-accounts/resolve',{method:'POST',body:JSON.stringify({ownLegalEntityId:'US01',accountServicerBic:'TESTUS33',currency:'USD',purpose:'SETTLEMENT',at:'2026-09-07'})});controls.push({control:'ENTITY_RESTRICTION_SECOND_LAYER',passed:denied.decision==='ENTITY_NOT_AUTHORIZED'&&allowed.decision==='RESOLVED',actual:{denied:denied.decision,allowed:allowed.decision}});
const pickerFunctions=[
  ['TRADE_FINANCE','IMPORT_LC','IMPORT_LC_BANK_REIMBURSEMENT','BANK_REIMBURSEMENT','pacs.009.001.12'],['TRADE_FINANCE','EXPORT_LC','EXPORT_LC_PROCEEDS_SETTLEMENT','PROCEEDS_SETTLEMENT','pacs.008.001.12'],['TRADE_FINANCE','IMPORT_COLLECTION','IMPORT_COLLECTION_PAYMENT','COLLECTION_SETTLEMENT','pacs.009.001.12'],['TRADE_FINANCE','EXPORT_COLLECTION','EXPORT_COLLECTION_PROCEEDS_SETTLEMENT','PROCEEDS_SETTLEMENT','pacs.008.001.12'],['TRADE_FINANCE','LC_REIMBURSEMENT','LC_REIMBURSEMENT_SETTLEMENT','BANK_REIMBURSEMENT','pacs.009.001.12'],['TRADE_FINANCE','STANDBY_LC','STANDBY_LC_CLAIM_PAYMENT','CLAIM_PAYMENT','pacs.009.001.12'],['TRADE_FINANCE','DEMAND_GUARANTEE','DEMAND_GUARANTEE_CLAIM_PAYMENT','CLAIM_PAYMENT','pacs.009.001.12'],['TREASURY','TREASURY_FX','FX_SETTLEMENT','INTERBANK_SETTLEMENT','pacs.009.001.12'],['TREASURY','TREASURY_MONEY_MARKET','MONEY_MARKET_SETTLEMENT','INTERBANK_SETTLEMENT','pacs.009.001.12'],['CENTRAL_PAYMENT','CENTRAL_PAYMENT','CUSTOMER_CREDIT_TRANSFER','CUSTOMER_TRANSFER','pacs.008.001.12'],
];
const activeEntities=(await request('booking-branch-entities')).filter((item)=>item.status==='ACTIVE');const genericRoutes=ssis.filter((item)=>item.route.sampleSet==='DEMO-GENERIC-FALLBACK');const coverageFailures=[];
let coverageCount=0;for(const generic of genericRoutes)for(const [consumer,product,businessFunction,paymentLeg,messageType] of pickerFunctions)for(const entity of activeEntities){coverageCount+=1;try{const preview=await request('resolve',{method:'POST',body:JSON.stringify({consumer,product,businessFunction,paymentLeg,direction:'OUTBOUND',counterpartyBic:'BARCGB22',counterpartyCountry:'GB',currency:generic.route.currency,bookingEntity:entity.branchCode,valueDate:'2026-09-07',amount:'1000000',messageType,transactionReference:`COVERAGE-${generic.route.currency}-${businessFunction}-${entity.branchCode}`})});if(!preview.recommendedRoute)coverageFailures.push(`${generic.route.currency}/${businessFunction}/${entity.branchCode}:${preview.decision}`);}catch(error){coverageFailures.push(`${generic.route.currency}/${businessFunction}/${entity.branchCode}:${error instanceof Error?error.message:String(error)}`);}}
const expectedEntityCodes=['HK01','SG01','GB01','US01','TW01','ZA01','AE01','DE01','CN01','KR01'];controls.push({control:'ALL_CURRENCY_FUNCTION_ENTITY_HAS_CANDIDATE',passed:genericRoutes.length===10&&activeEntities.length===10&&expectedEntityCodes.every((code)=>activeEntities.some((entity)=>entity.branchCode===code))&&activeEntities.some((entity)=>entity.branchCode==='TW01'&&entity.legalEntityCode==='DEMO-TW'&&entity.countryCode==='TW')&&coverageCount===1000&&coverageFailures.length===0,actual:{currencies:genericRoutes.length,functions:pickerFunctions.length,entities:activeEntities.length,combinations:coverageCount,failures:coverageFailures.slice(0,10)}});
const usdExport=await request('resolve',{method:'POST',body:JSON.stringify({consumer:'TRADE_FINANCE',product:'EXPORT_LC',businessFunction:'EXPORT_LC_PROCEEDS_SETTLEMENT',paymentLeg:'PROCEEDS_SETTLEMENT',direction:'OUTBOUND',counterpartyBic:'BARCGB22',counterpartyCountry:'GB',currency:'USD',bookingEntity:'HK01',valueDate:'2026-09-07',amount:'1000000',messageType:'pacs.008.001.12',transactionReference:'USD-BARC-EXPORT-LC-FALLBACK'})});controls.push({control:'USD_BARC_EXPORT_LC_USES_GENERIC_FALLBACK',passed:usdExport.recommendedRoute?.route?.sampleSet==='DEMO-GENERIC-FALLBACK'&&usdExport.recommendedRoute?.route?.routePreference==='FALLBACK',actual:usdExport.recommendedRoute?.route?.ssiCode});
const clearingSystems=(await request('reference/clearing-systems')).items;const clearingByCode=new Map(clearingSystems.map((system)=>[system.code,system]));
const requiredClearing=['HKD_CHATS','USD_CHATS','EUR_CHATS','RMB_CHATS','FPS_HKD','FPS_RMB','FEDWIRE','CHIPS','T2','EURO1','STEP2_SCT','TIPS','RT1','RPS_SEPA'];controls.push({control:'CLEARING_STANDING_DATA_BASELINE',passed:requiredClearing.every((code)=>clearingByCode.has(code))&&!clearingByCode.has('TARGET')&&!clearingByCode.has('TARGET2'),actual:{records:clearingSystems.length,missing:requiredClearing.filter((code)=>!clearingByCode.has(code))}});
const invalidActiveSsis=ssis.filter((ssi)=>{const system=clearingByCode.get(ssi.route.clearingSystem);return !system||ssi.route.clearingSystem==='ANY'||system.status!=='ACTIVE'||system.supportedCurrency!==ssi.route.currency||system.schemeType!==ssi.route.schemeType;});controls.push({control:'EVERY_ACTIVE_SSI_HAS_CONCRETE_COMPATIBLE_CLEARING',passed:invalidActiveSsis.length===0,actual:invalidActiveSsis.map((ssi)=>ssi.route.ssiCode)});
const usdSpecific=ssis.find((ssi)=>ssi.route.ssiCode==='SSI-DEMO-001');if(usdSpecific){const app=usdSpecific.applicability[0];const base={consumer:app.consumer,product:app.product,businessFunction:app.businessFunction,paymentLeg:app.paymentLeg,direction:app.direction,counterpartyBic:usdSpecific.route.counterpartyBic,counterpartyCountry:usdSpecific.route.counterpartyCountry,currency:'USD',bookingEntity:'HK01',valueDate:'2026-09-07',amount:'1000000',messageType:usdSpecific.route.messageTypes,transactionReference:'HK01-USD-MULTI-RAIL'};const preview=await request('resolve',{method:'POST',body:JSON.stringify(base)});const codes=[preview.recommendedRoute,...preview.alternatives].filter(Boolean).map((item)=>item.route.clearingSystem);const hkPreview=await request('resolve',{method:'POST',body:JSON.stringify({...base,settlementCountry:'HK',transactionReference:'HK01-USD-HK-OVERRIDE'})});const hkCodes=[hkPreview.recommendedRoute,...hkPreview.alternatives].filter(Boolean).map((item)=>item.route.clearingSystem);controls.push({control:'HK01_USD_MULTI_RAIL_NOT_ENTITY_FORCED',passed:codes.includes('FEDWIRE')&&codes.includes('USD_CHATS'),actual:codes});controls.push({control:'HK_SETTLEMENT_OVERRIDE_ONLY_HK_RAILS',passed:hkCodes.length>0&&hkCodes.every((code)=>clearingByCode.get(code)?.settlementCountry==='HK'),actual:hkCodes});}
const codesFor=(currency)=>clearingSystems.filter((system)=>system.supportedCurrency===currency).map((system)=>system.code);const usdCodes=codesFor('USD'),eurCodes=codesFor('EUR'),hkdCodes=codesFor('HKD'),cnyCodes=codesFor('CNY');controls.push({control:'CLEARING_CURRENCY_DEPENDENCIES',passed:usdCodes.includes('FEDWIRE')&&usdCodes.includes('CHIPS')&&usdCodes.includes('USD_CHATS')&&!usdCodes.includes('T2')&&!usdCodes.some((code)=>code.startsWith('FPS_'))&&eurCodes.includes('T2')&&eurCodes.includes('EUR_CHATS')&&hkdCodes.includes('FPS_HKD')&&cnyCodes.includes('FPS_RMB')&&!clearingSystems.filter((system)=>system.code.startsWith('FPS_')).some((system)=>!['HKD','CNY'].includes(system.supportedCurrency)),actual:{USD:usdCodes,EUR:eurCodes,HKD:hkdCodes,CNY:cnyCodes}});
const eurDe=(await request('reference/clearing-systems?currency=EUR&settlementCountry=DE')).items.map((system)=>system.code);const eurHk=(await request('reference/clearing-systems?currency=EUR&settlementCountry=HK')).items.map((system)=>system.code);controls.push({control:'PAN_EUROPEAN_COUNTRY_SCOPE',passed:['T2','EURO1','STEP2_SCT','TIPS','RT1','RPS_SEPA'].every((code)=>eurDe.includes(code))&&!eurDe.includes('EUR_CHATS')&&!eurDe.includes('TARGET')&&!eurDe.includes('TARGET2')&&eurHk.includes('EUR_CHATS')&&!eurHk.includes('T2'),actual:{DE:eurDe,HK:eurHk}});
const eurHighValue={consumer:'TRADE_FINANCE',product:'IMPORT_LC',businessFunction:'IMPORT_LC_BANK_REIMBURSEMENT',paymentLeg:'BANK_REIMBURSEMENT',direction:'OUTBOUND',counterpartyBic:'DEUTDEFF',counterpartyCountry:'DE',settlementCountry:'DE',currency:'EUR',bookingEntity:'DE01',valueDate:'2026-09-07',amount:'1000000',messageType:'pacs.009.001.12',transactionReference:'EUR-DE-HIGH-VALUE'};const eurEligibleOptions=(await request('resolve/clearing-options',{method:'POST',body:JSON.stringify(eurHighValue)})).items.map((system)=>system.code);const eurHighPreview=await request('resolve',{method:'POST',body:JSON.stringify(eurHighValue)});controls.push({control:'EUR_HIGH_VALUE_SSI_BACKED_OPTIONS_AND_RANKING',passed:eurEligibleOptions.length===2&&eurEligibleOptions.includes('T2')&&eurEligibleOptions.includes('EURO1')&&!['STEP2_SCT','TIPS','RT1','RPS_SEPA'].some((code)=>eurEligibleOptions.includes(code))&&eurHighPreview.recommendedRoute?.route?.ssiCode==='SSI-DEMO-005'&&eurHighPreview.alternatives.some((item)=>item.route.ssiCode==='SSI-DEMO-035'),actual:{options:eurEligibleOptions,recommended:eurHighPreview.recommendedRoute?.route?.ssiCode,alternatives:eurHighPreview.alternatives.map((item)=>item.route.ssiCode)}});
const nonPaymentOptions=await request('resolve/clearing-options',{method:'POST',body:JSON.stringify({...eurHighValue,businessFunction:'IMPORT_LC_OPENING',paymentLeg:'MESSAGE_OPENING',transactionReference:'EUR-NON-PAYMENT'})});controls.push({control:'PAYMENT_TRIGGER_REQUIRED',passed:nonPaymentOptions.decision==='PAYMENT_TRIGGER_REQUIRED'&&nonPaymentOptions.items.length===0,actual:nonPaymentOptions});
const gbpPrimary={consumer:'TRADE_FINANCE',product:'EXPORT_LC',businessFunction:'EXPORT_LC_PROCEEDS_SETTLEMENT',paymentLeg:'PROCEEDS_SETTLEMENT',direction:'OUTBOUND',counterpartyBic:'BARCGB22',counterpartyCountry:'GB',settlementCountry:'GB',currency:'GBP',bookingEntity:'GB01',valueDate:'2026-09-07',amount:'1000000',messageType:'pacs.008.001.12',transactionReference:'GBP-GB-CHAPS'};const gbpOptions=(await request('resolve/clearing-options',{method:'POST',body:JSON.stringify(gbpPrimary)})).items.map((system)=>system.code);const gbpPreview=await request('resolve',{method:'POST',body:JSON.stringify(gbpPrimary)});controls.push({control:'GBP_GB_CHAPS_PRIMARY',passed:gbpOptions.length===1&&gbpOptions[0]==='CHAPS'&&gbpPreview.recommendedRoute?.route?.ssiCode==='SSI-DEMO-003'&&gbpPreview.recommendedRoute?.route?.routePreference==='PRIMARY',actual:{options:gbpOptions,recommended:gbpPreview.recommendedRoute?.route?.ssiCode,routeClass:gbpPreview.recommendedRoute?.route?.routePreference}});
const duplicateKeys=new Map();for(const ssi of ssis){for(const app of ssi.applicability??[]){const key=[ssi.route.counterpartyBic,ssi.route.currency,app.consumer,app.product,app.businessFunction,app.paymentLeg,app.direction,ssi.route.clearingSystem,ssi.route.settlementRouteId,ssi.route.routePreference,ssi.route.priority].join('|');duplicateKeys.set(key,[...(duplicateKeys.get(key)??[]),ssi.route.ssiCode]);}}const accidental=[...duplicateKeys.values()].filter((ids)=>ids.length>1);controls.push({control:'NO_ACCIDENTAL_ACTIVE_DUPLICATES',passed:accidental.length===0,actual:accidental});
if(controls.some((item)=>!item.passed))failures.push({label:'PURPOSE_BUILT_CONTROLS',error:JSON.stringify(controls)});
const summary={activeSupportedSsis:ssis.length,passed:results.length,failed:failures.length,purposeBuiltControls:controls,failures};console.log(JSON.stringify(summary,null,2));if(failures.length)process.exitCode=1;

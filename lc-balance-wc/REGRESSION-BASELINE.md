# Regression Baseline — Balance Component OOD Refactoring

Established per the Senior Engineer Refactoring & Regression-Safety Requirements (2026-08-19), before
any Phase-1 (F-01) production code change. This is the "Known Baseline" every subsequent Phase's
"After Refactoring" result must be compared against.

**Baseline commit**: `450e6f8f51ce5835d17eb6c912ba83276f675dcc` (2026-08-19 10:03:45 +0800)

## 1. Build / Compile

| Sub-project | Command | Result |
|---|---|---|
| Angular app | `npx tsc -p tsconfig.app.json --noEmit` | PASS, 0 errors |
| Microservice | `npm run typecheck` (`tsc --noEmit`) | PASS, 0 errors |
| Backend | n/a (plain JS) | — |

## 2. Unit / Integration Test Suites

| Sub-project | Tests | Coverage (Stmts / Branch / Funcs / Lines) | Floor |
|---|---|---|---|
| Angular app | 821/821 passing, 19 suites | 99.37% / 96.28% / 99.35% / 99.48% | 95% |
| Microservice | 322/322 passing, 13 suites | 99.25% / 97.16% / 100% / 99.49% | 95% |
| Backend | 34/34 passing, 3 suites | 97.32% / 95.34% / 96.42% / 98.03% | 95% |

All three clear their own established coverage floor. Zero failing tests.

## 3. Business Case Registry — end-to-end live scenarios (Import A-series + Export B-series)

All 14 registered Business Cases executed live against the real running microservice/backend
(`POST /api/business-cases/:id/run`), spaced to respect the microservice's own 120 req/min rate
limiter (a same-session, already-known false-positive source when cases are run back-to-back — see
`CLAUDE.md`'s own prior notes on this; ruled out explicitly this pass by re-running the one case that
initially reported an error, after a full cooldown window, and confirming it then passes clean with
`"ok":false` count `0` across its entire step trace).

| Case | Scenario | Result |
|---|---|---|
| import-case-1 | A1 Issue → A2 Amendment → A3 Document Arrival → A4/A6 Settlement | PASS |
| import-case-2 | A-series variant | PASS |
| import-case-3 | A-series variant (A3S / SG-matched Document Arrival) | PASS |
| import-case-4 | A-series variant (A3S SG-netting) | PASS |
| import-case-5 | A2 Amendment Decrease past Available — **expected 409** (`expectError: true`, the file's only intentional negative case) | PASS (expected failure occurred exactly as designed) |
| import-case-6 | A1→A8 SG→A3/A3S Document Arrivals→real A4 Maker-Submit→Checker Release (×3) | PASS |
| import-case-7 | A1 Usance→A3 Document Arrival→A8 SG→A3S→A6 Acceptance (×2)→A7 Settlement | PASS |
| export-case-1 | B-series baseline | PASS |
| export-case-2 | B-series variant | PASS |
| export-case-3 | B-series variant | PASS |
| export-case-4 | B-series variant | PASS |
| export-case-5 | Usance + Unconfirmed + EBL financing | PASS (confirmed clean on isolated re-run after rate-limit cooldown — zero `"ok":false` steps) |
| export-case-6 | B1 Confirm→B3 Present Docs (memo earmark)→B4 Honour (unified legal event)→Due From Issuing Bank | PASS |
| export-case-7 | B1 Confirm Usance→B3→B4 Accept (compound)→B5 Settlement (compound) | PASS |

**14/14 cases pass; the 1 intentionally-designed negative case (`import-case-5`) fails exactly as
designed and only as designed.**

Test data generated by this baseline run (`IMP-C%`/`EXP-C%` naturalKey pattern, 26 contract rows) was
cleaned up afterward. Verified before/after: all 41 reference-LC contract rows (S01–S05, S07–S11, U01,
U02, both Import and Export sides) remained byte-for-byte untouched throughout.

## 4. API contract / balance / event-timeline behavior

Spot-checked via the traces above (each step's own `request`/`response` body, e.g. `contingentAccountEntry`,
`eventSnapshot`, `balanceBefore`/`balanceAfter`, `status` transitions) — all match the shapes and values
already documented in `CLAUDE.md`'s own decision log for these same cases. No unexplained values observed.

## How to reproduce this baseline

```bash
# Angular app
cd lc-balance-wc && npx tsc -p tsconfig.app.json --noEmit && npm run test:coverage

# Microservice
cd lc-balance-wc/microservices/balance-component && npm run typecheck && npm run test:coverage

# Backend
cd lc-balance-wc/backend && npm run test:coverage

# Business Case Registry (requires all three dev processes running — npm run dev:all)
# Run each of the 14 case ids one at a time (respect the 120 req/min rate limiter — space calls out,
# don't fire all 14 back-to-back), via:
curl -X POST http://localhost:4300/api/business-cases/<case-id>/run
# Clean up afterward: DELETE balance_movements/balance_contracts WHERE lc_number LIKE 'IMP-C%' OR 'EXP-C%'
```

---

*This file is a point-in-time record, not living documentation — do not edit it to reflect new work;
create a new dated comparison note instead when the next Phase's "After Refactoring" report is ready,
and update this file's own "baseline commit" only if a NEW baseline is deliberately re-established.*

## 0. Review Gate

- [x] 0.1 Obtain OpenSpec Change Review／Approval for `proposal.md`, `design.md`, all delta specs, `tasks.md` and `requirement-traceability.md` before implementation.
- [x] 0.2 Confirm BD-01 and BD-02 in review evidence; do not add `FX_RATE_PENDING` or use the virtual adapter in production.
- [x] 0.3 Obtain amended OpenSpec Change Approval for BD-03 Zero Allowance Legacy Fallback before implementing BD-03 behavior.
- [x] 0.4 Historical approval for V4 scope（PASS, 2026-09-22）; its calculable over-limit Submit／Block contract was superseded by task 0.6／revised BD-06 on 2026-09-23, while the remaining Formal Increase、A3S guidance and PBD boundaries remain applicable.
- [x] 0.5 Obtain amended OpenSpec Change Approval for BD-07／BD-08: no Return Documents／partial return／partial cancellation／Approved Excess reversal; Delete Pending only removes the whole `PENDING|REJECTED` transaction and releases its whole Pending Excess Reservation; Fix Pending atomically replaces `old amount/reservation` with `new amount/recalculated reservation` under the same identity. PASS granted by user on 2026-09-22; affected implementation may resume on `OVERDRAWN` under TDD.
- [x] 0.6 Confirm BD-06 supersession and BD-09 on 2026-09-23: A8／A3／A3S／B3 over-limit Maker Submit is HTTP `409 EXCESS_LIMIT_EXCEEDED` zero-write; A3／A3S retain Pending Excess through Acknowledge and convert only at A4／A6 final Release; A3S SG redemption／capacity／attribution commits once at Acknowledge.
- [x] 0.7 Confirm BD-10 on 2026-09-23: post-Acknowledge A3S Amount Fix is prohibited because once-only SG redemption／capacity／attribution facts have committed; UI protects Amount and API rejects with HTTP `409 ILLEGAL_STATE_TRANSITION` without FX or writes. Remarks-only correction remains governed by existing function policy.

## 1. Contracts, Configuration and Persistence — Tests First

- [x] 1.1 Add failing configuration tests and implement versioned `ExcessPolicyConfig` under `microservices/balance-component/src/config.ts` plus a reviewed config file under `microservices/balance-component/config/`; test missing, overlap, effective date, maximum, percentage and `FAIL_CLOSED` freshness policy.
- [x] 1.2 Add failing migration／constraint tests in `microservices/balance-component/test/unit/db/` before extending `microservices/balance-component/src/db/schema.ts` and `migrations.ts` with the originally approved persistence model.
- [x] 1.3 Add typed wire／domain models and exact enums in `microservices/balance-component/src/types.ts`; keep Excess decision, workflow, accounting, contract and FX command result states separate.
- [x] 1.4 Add store contract tests before implementing narrow stores in `microservices/balance-component/src/store/`; prove owner versioning, immutable events, aggregate derivation and migration preservation.
- [x] 1.1a Add failing config tests then extend the versioned effective-dated policy schema for PBD authorization, including missing／false／authorized／effective-date cases and exact `fallbackPolicyId`／`fallbackPolicyVersion` evidence（TDD RED/GREEN; 23 focused tests including inclusive-start／open-ended boundaries; full 48-suite／944-test regression PASS, 2026-09-22）.
- [x] 1.2a Remediate the pre-V4-draft Formal Increase allocation capability: add failing negative tests, remove `FORMAL_INCREASE_REGULARIZATION` from active command／write enums、DTOs、store APIs and aggregate logic, and prevent new allocation writes. Preserve any already-migrated append-only table/data plus a read-only legacy decoder through a compatibility migration rather than destructive deletion; update fresh-schema and upgrade tests so no active cure capability remains（TDD RED/GREEN; migration 28; 14 focused tests and full 48-suite／944-test regression PASS, 2026-09-22）.

## 2. Pure Policies and Currency Exchange Port — Tests First

- [x] 2.1 Add `excessPolicy.test.ts` then implement pure exact-decimal Covered／Excess split and owner allowance policy under `microservices/balance-component/src/domain/`.
- [x] 2.2 Add boundary tests for zero capacity, owner-currency exact limit, one minor unit over, owner-currency rounding and concurrent aggregate inputs; use `microservices/balance-component/src/money.ts`, never JavaScript number. Replace any pre-V4-draft transaction→USD conversion work with the approved owner-currency formula. — TDD evidence: `evidence/task-2.2-tdd.md`; BA PASS, independent 4-Eyes PASS, QA PASS; 48 suites / 947 tests PASS.
- [x] 2.3 Add function strategies for A8／A3／A3S／B3 covered-capacity inputs without duplicating allowance arithmetic; add invariant tests requiring A8／A3／A3S currency=Import LC currency and B3 currency=Confirmation currency, with pre-FX zero-write rejection on mismatch. — TDD evidence: `evidence/task-2.3-tdd.md`; BA PASS, independent 4-Eyes PASS, QA PASS; 50 suites / 962 tests PASS.
- [x] 2.4 Add negative contract tests then remove active `RETURN_DOCUMENTS`、`RETURN_REVERSAL`、`CANCELLATION_REVERSAL` and partial reversal policy capability. Prove Delete Pending accepts no amount, acts on the whole `PENDING|REJECTED` movement, releases the whole Pending Excess Reservation and cannot reduce Approved Excess; preserve any pre-existing draft rows only as read-only legacy audit evidence. — TDD evidence: `evidence/task-2.4-tdd.md`; BA PASS, independent 4-Eyes PASS, independent QA PASS; focused 4 suites／21 tests and full 52 suites／973 tests PASS; coverage S97.99／B95.15／F98.91／L98.56.
- [x] 2.5 Define `CurrencyExchangePort` and DTOs under `microservices/balance-component/src/integration/`; add baseline contract tests for BOOKING purpose, Approved／Effective, freshness, correlation, source／version and USD par.
- [x] 2.5a Amend port／DTO tests and implementation to require `fromCurrency=USD`、`toCurrency=ownerCurrency`、`amount=configuredMaximumUsd`; consume provider `convertedAmount`, validate direction／amount correlation, prohibit Balance-side inverse conversion, and use `USD_PAR` identity for USD owners. — TDD evidence: `evidence/task-2.5a-tdd.md`; BA PASS, independent 4-Eyes PASS, independent QA PASS; focused 2 suites／57 tests and full 52 suites／981 tests PASS; coverage S97.99／B95.18／F98.91／L98.57.
- [x] 2.6 In `../lc-payment-wc/backend/data/fx-rates.json` and `../lc-payment-wc/backend/server.js`, first add tests then extend the non-production virtual endpoint to emit exact-decimal `buyRate`, `sellRate`, optional `bookingRate`, deterministic metadata and `bookingRate=(buyRate+sellRate)/2` when omitted; preserve legacy fixtures by explicitly setting both sides where appropriate.
- [x] 2.6a Add failing virtual-provider tests, then add direct USD→owner fixtures and update the non-production endpoint to accept `fromCurrency=USD`、`toCurrency=ownerCurrency`、`amount=configuredMaximumUsd`, return provider `convertedAmount` rounded once to target owner-currency precision, and cover explicit Booking／midpoint／missing-side／stale／not-Approved／not-Effective／timeout cases. USD owners use Balance-side `USD_PAR` with zero provider calls; Balance MUST NOT invert legacy owner→USD fixtures. — TDD evidence: `evidence/task-2.6a-tdd.md`; BA PASS, independent 4-Eyes PASS, independent QA PASS; Payment focused 1 suite／43 tests, Payment backend 2 suites／47 tests, Balance full 52 suites／981 tests, frontend 12 suites／354 tests PASS; virtual provider coverage S98.41／B94.31／F100／L100.
- [x] 2.7 Implement a Balance virtual adapter and production boundary; reject the virtual adapter in production and never derive provider Booking from Buy／Sell. Permit provider-supplied PBD `BOOKING` only under explicit FX Policy authorization plus Approved／Effective／Max Staleness checks and complete fallback audit evidence. — TDD evidence: `evidence/task-2.7-tdd.md`; BA PASS, independent 4-Eyes PASS, independent QA PASS; focused 2 suites／67 tests and full 53 suites／1015 tests PASS; coverage S98.02／B95.24／F98.92／L98.59, `currencyExchange.ts` S100／B99.23／F100／L100.
- [x] 2.8 Test timeout／retry／late／out-of-order handling; keep retry command correlation／idempotency stable while generating a distinct `requestAttemptId` per provider call, require exact provider echo matching, treat `providerRateVersion` as opaque audit evidence only, and map unavailable／not Approved／not Effective／attempt mismatch to `FX_RATE_UNAVAILABLE`, stale to `FX_RATE_STALE`. — TDD evidence: `evidence/task-2.8-tdd.md`; BA PASS, independent 4-Eyes PASS, independent QA PASS; Balance focused 3 suites／91 tests and full 54 suites／1039 tests PASS; Payment FX 44 tests and backend 48 tests PASS; coverage S97.95／B95.12／F98.94／L98.61.
- [x] 2.9 Add pure routing tests proving `configuredMaximumUsd = 0 OR allowancePercentage = 0` selects legacy sufficiency before Covered／Excess or FX, while both values greater than zero select the Excess Framework.

## 3. Maker／Checker and Allowance Ledger — Tests First

- [x] 3.1 Add service tests for the Maker sequence: idempotency, current facts, config, split, FX, locked allowance, atomic persistence. — TDD evidence: `evidence/task-3.1-tdd.md`; Trade Finance BA PASS, independent 4-Eyes PASS, independent QA PASS; focused 1 suite／17 tests and full Balance 55 suites／1056 tests PASS; coverage S97.91／B95.10／F98.95／L98.64.
- [x] 3.2 Implement owner account orchestration and Pending Excess Reservation in new narrow services／stores; integrate through `microservices/balance-component/src/service/balanceService.ts` and `unitOfWork.ts`.
- [x] 3.3 Historical pre-revision implementation evidence: failure atomicity remains relevant, while its HTTP `201`／persisted blocked-over-limit behavior was superseded by revised BD-06 and MUST be removed by task 3.3a.
- [x] 3.3a Supersede the completed pre-BD-06 implementation under TDD: first change A8／A3／A3S／B3 unit／integration／API tests to require HTTP `409 EXCESS_LIMIT_EXCEEDED` and zero movement、reservation、FX／decision snapshot、ledger、audit-as-transaction fact and idempotent success; then remove all persisted `LIMIT_EXCEEDED／BLOCKED` Submit behavior. Add concurrent last-slot proof that at most one command persists and every over-limit loser is zero-write. — TDD evidence: `evidence/task-3.3a-tdd.md`; completed after four-function HTTP coverage in Task 5.2.
- [x] 3.4 Extend `movementReleasePolicyService.test.ts` first, then re-read contract、linked facts、policy、allowance and latest Booking Rate at Checker Release.
- [x] 3.5 Prove Checker unavailable／stale rate retains the pending movement and reservation while recording only a command-attempt audit. — TDD evidence: `evidence/task-3.5-tdd.md`; unavailable／stale retain pending facts and append only an idempotent Checker command-attempt audit; invalid actor／state reject pre-FX; 58 suites／1122 tests PASS, coverage S97.69／B95.02／F99.05／L98.51.
- [x] 3.6 Under TDD, atomically convert reservation to Approved Excess with release side effects in `movementReleaseSideEffectService.ts` and `unitOfWork.ts`: A8／B3 convert at their own Checker Release; A3／A3S Acknowledge revalidates but keeps LC UTILIZE and reservation Pending; Sight A4／Usance A6 final Release revalidates again and atomically converts／finalises. Preserve A3S SG redemption、capacity consumption and attribution as a once-only Acknowledge side effect and prove A4／A6 never repeat it. — TDD evidence: `evidence/task-3.6-bd09-bd10-partial-tdd.md`; full regression 60 suites／1,189 tests PASS.
- [x] 3.7 Extend Fix／Resubmit tests and service so Amount is mutable only for pending A8／A3／B3 and `acknowledgedAt = null` A3S; reuse original business-event／pending identity and atomically calculate `current pending total - old pending amount + new pending amount`, with the matching `current pending excess - old reservation + new recalculated reservation`. Include the approved A3 examples `10200/200 → 10300/300` and `10200/200 → 10100/100`; prove the old `200` is excluded once, never added to the new result, no old amount／reservation is removed before complete revalidation and every failure preserves the original facts. Add post-Acknowledge A3S negative tests for both `PENDING`／EARMARKED and `REJECTED`, requiring HTTP `409 ILLEGAL_STATE_TRANSITION` before policy／FX, zero FX and no mutation of movement／reservation／snapshots／once-only SG facts. — TDD evidence: `evidence/task-3.7-tdd.md`.
- [x] 3.8 Extend Reject／Delete tests and `deletePendingAudit.ts`: Reject retains reservation and rejected facts; Delete Pending accepts `PENDING` or `REJECTED`, atomically transitions to `DELETED`, releases reservation and preserves both reject／deletion audit; approved facts cannot be deleted. — TDD evidence: `evidence/task-3.8-tdd.md`; A3／A3S／A8／B3, atomic rollback and Delete idempotency covered; independent 4-Eyes PASS, independent QA PASS; focused 2 suites／178 tests and full 60 suites／1,206 tests PASS.
- [x] 3.9 Prove existing A2／B2 processing is unchanged and a fresh Submit after A2／B2 Increase reads only the latest Checker-released Approved Contractual Maximum. Implement Minimum Required Increase error guidance with the formal predicate, including partial-increase-still-zero-write-rejected, no-finite-solution and FX-contribution cases. Prove no blocked over-limit movement、A2／B2 auto-create、`FORMAL_INCREASE_REGULARIZATION`、allocation／cure event or Approved Excess reduction exists. — TDD evidence: `evidence/task-3.9-tdd.md`; A8／A3／A3S／B3 initial Submit and Fix Pending guidance, actual A2／B2 Pending／partial／sufficient Increase lifecycle, provider-FX contribution, no-finite outcome, immutable Approved Excess and sequential A3S re-selection covered; 61 suites／1,221 tests PASS.
- [x] 3.10 Add negative application tests proving no Import／Export Return Documents or A8 partial cancellation command exists; keep all full withdrawal behavior exclusively in Task 3.8 Delete Pending. — TDD evidence: `evidence/task-3.10-tdd.md`; cancellation amount is explicitly rejected with zero mutation, obsolete Return／partial-cancel routes remain absent, and full regression is 61 suites／1,224 tests PASS.
- [x] 3.11 Add A8／A3／A3S／B3 service tests and implement BD-03 legacy routing with existing `INSUFFICIENT_AVAILABLE_BALANCE` code／message, zero FX interaction and zero Excess persistence. — TDD evidence: `evidence/task-3.11-tdd.md`; four-function HTTP characterization confirms the existing legacy route, 61 suites／1,228 tests PASS.

## 4. SG Capacity and Downstream Eligibility — Tests First

- [x] 4.1 Add A8 approval tests then initialize Eligible SG Capacity with Covered／Excess attribution independently of SHGT legal balance. — TDD evidence: `evidence/task-4.1-tdd.md`; Covered-only and partial-Excess A8 approvals initialize one append-only capacity event while preserving the independent SHGT legal balance; 61 suites／1,229 tests PASS.
- [x] 4.2 Add A3S partial／full／over-capacity tests before changing `offBalanceExposure.ts`, `shgtRedeem.ts` and release side effects. — Consolidated TDD evidence: `evidence/task-4.2-4.6-tdd.md`; exact capacity reservation／redemption, selection guidance and zero-write over-capacity paths covered.
- [x] 4.3 Prove A8→A3S transfers attribution exactly once and only the amount above SG capacity receives a new parent Covered／Excess decision. — Consolidated TDD evidence: `evidence/task-4.2-4.6-tdd.md`; BD-11 Excess-first partial transfer and parent／allowance anti-double-counting covered.
- [x] 4.4 Prove A3S capacity／attribution consumption itself does not imply SG legal／contingent-liability release; preserve the separate explicit linked SG redemption once at A3S Acknowledge, and prove A4／A6 never repeat it. Prove A9 does not automatically release Approved Excess. — Consolidated TDD evidence: `evidence/task-4.2-4.6-tdd.md`; Acknowledge owns the once-only legal redemption, final Release does not repeat it, and standalone A9 preserves Approved Excess／capacity facts.
- [x] 4.5 Add negative SG-capacity tests proving no Return／partial cancellation restoration path exists; only a full pre-approval Delete Pending may roll back the complete pending movement side effects atomically. — Consolidated TDD evidence: `evidence/task-4.2-4.6-tdd.md`; pre-Acknowledge whole Delete atomically cancels both A3S legs and reverses the exact capacity reservation, while post-Acknowledge committed SG facts remain unchanged.
- [x] 4.6 Extend A4／A6／B4／A9 eligibility and compound service tests so downstream completion preserves Approved Excess and remains atomic. — Consolidated TDD evidence: `evidence/task-4.2-4.6-tdd.md`; focused 88／88 and full microservice 62 suites／1,240 tests PASS.

## 5. Simplified API／OAS Gate — TDD

- [x] 5.S1 Preserve the implemented typed HTTP vertical slice for A8／A3／A3S／B3, Checker decision points, BD-10, virtual FX bootstrap and final Release. — Existing evidence: `evidence/task-5.2-a3-http-partial-tdd.md` plus Task 3／4 evidence.
- [x] 5.S2 One consolidated API contract pack SHALL cover request authority、typed success/errors、Minimum Required Increase、same-key replay／payload conflict、compound atomicity、owner concurrency、BD-03 legacy errors、whole Delete Pending inquiry and absence of Return／partial cancellation／cure endpoints. Update and validate `analysis/balance-component-api.yaml`; reuse existing tests and add only missing contract assertions. This replaces legacy tasks 5.1、5.3–5.7 without reducing scope. — TDD evidence: `evidence/task-5.S2-tdd.md`; focused HTTP／OpenAPI 2 suites／76 tests, YAML parse, typecheck, lint, diff-check and OpenSpec strict 16／16 PASS.

## 6. Simplified Angular Gate — TDD

- [x] 6.S1 One consolidated Angular test pack SHALL type and render the existing Excess／FX responses, Covered／Excess preview and audit facts; protect Amount after A3S Acknowledge; present Fix as whole replacement; display `FX_RATE_UNAVAILABLE`、`FX_RATE_STALE` and zero-write `EXCESS_LIMIT_EXCEEDED`; never display `FX_RATE_PENDING`; and enforce sequential A3S re-selection before Minimum Required Increase. Reuse the existing Transaction Builder and result/error components—no new screen or framework. This replaces legacy tasks 6.1–6.5. — TDD evidence: `evidence/task-6.S1-tdd.md`; focused 5 suites／560 tests, typecheck and lint PASS.

## 7. Simplified Regression／Business Case Runner Gate

- [x] 7.S1 Add the smallest deterministic `OVERDRAWN` Business Case Runner matrix that proves: one Covered／partial／exact／over-limit route across A8／A3／A3S／B3; Runner-only automatic Import A02 and Export B02 Increase after exact HTTP `409 EXCESS_LIMIT_EXCEEDED`, then fresh Submit; A8→A3S anti-double-counting; A4／A6／B4／A9 preserve Approved Excess; virtual midpoint is non-production only; provider Booking／Freshness fail-closed; BD-03 zero-value legacy behavior; whole Delete only; idempotency／concurrency. Production UI／API remains zero-write/no-auto. Prefer parameterized shared assertions over Cartesian duplication. This replaces legacy tasks 7.1–7.7 and 7.11–7.12. — TDD evidence: `evidence/task-7.S1-tdd.md`; five compact OVERDRAWN journeys reuse the existing parameterized negative／concurrency suites; backend 3 suites／71 tests PASS, coverage S98.12／B95.71／F100／L99.59, lint PASS; real-service A3／A8／A3S／B3 remediation trace PASS.
- [x] 7.S2 Run and retain only the release-relevant full gates: Balance microservice tests／coverage, Angular tests／coverage, backend tests, Business Case Runner single-case plus Run All against the real Balance microservice and virtual FX adapter. Existing A1–A11／B1–B7 characterization must remain green. This replaces legacy tasks 7.8–7.10. — Evidence: `evidence/task-7.S2-full-regression.md`; Angular 2043／2043, microservice 1247／1247, backend 71／71, virtual FX 44／44 and real-service Run All 42／42 PASS. Historical microservice Jest 95%-per-metric threshold remains unchanged and is transparently deferred to the approved Task 8.S2 Sonar 92% release gate.

## 8. Simplified Documentation／Quality／Approval Gate

- [x] 8.S1 Update only the affected FX、A3／A3S／B3、Maker／Checker、data model、downstream and traceability pages; bind every requirement row to existing or new evidence; validate OAS and run `openspec validate --all --strict --no-interactive`. No duplicate narrative documents. — 2026-09-24 strict validation: 16 PASS／0 FAIL; OAS retains Applicant Waiver fields as deprecated legacy compatibility only，includes B4 authorization／asset receipts and excludes A8 from the active Excess contract.
- [ ] 8.S2 Run one reproducible ARM64 SonarQube release-candidate scan using the reviewed `../lc-ssi-wc/qa/docker` pattern, project key `lc-balance-wc`, `sonar.qualitygate.wait=true`, fresh LCOV files and ephemeral secrets. Retain SHA／clean-tree／LCOV hashes／analysis ID evidence and pass the approved New Code and Overall Code thresholds exactly as shown in the accepted Quality Gate screenshots. This consolidates legacy tasks 8.4a–8.4d.
- [ ] 8.S3 Obtain final Trade Finance BA、4-Eyes and independent QA PASS. After approval only, archive `unified-lc-excess-framework`, re-run strict validation and verify current specs. This replaces legacy task 8.5.

## 9. FROZEN V2 Change Update（Planning Only）

This section supersedes the earlier Task 9 review result. Every A8／A9／BD-11／A3S SG Redemption behavior item in completed historical Tasks 0–8 remains `OUT_OF_SCOPE`; historical checkmarks and evidence are audit-only. **No item below authorizes implementation before Task 9.6 Change Approval PASS.**

- [x] 9.1 Read the complete FROZEN `信用證超押處理業務需求_v11.15_V4_BA_REVIEW_FIXED_V2.docx` and record it as the sole business baseline for this update.
- [x] 9.2 Update `proposal.md`、`design.md` and every affected delta spec: preserve main A3S SG behavior; define normalized Base Parent Tight + Current SG Redemption calculation、Acknowledge locked split、A4／A6 no re-split、A3／A3S post-Acknowledge Amount immutability、event invariants and B4 authorization.
- [x] 9.3 Update `requirement-traceability.md` and this task plan; mark the old selected-capacity Excess basis、B3-only authorization／B4 read-only contract and `EXCESS_RECOURSE_ASSET` name as `SUPERSEDED`.
- [x] 9.4 Run `openspec validate --all --strict --no-interactive` and document the complete result（2026-09-23：16 PASS／0 FAIL）。
- [x] 9.5 Obtain Trade Finance BA、independent 4-Eyes and independent QA PASS against FROZEN V2, with no unresolved Business Decision（2026-09-23：BA PASS、4-Eyes PASS、QA PASS；P0／P1／P2 = 0／0／0；Covered-only capacity P1 closed）。
- [x] 9.6 Obtain renewed OpenSpec Change Approval PASS（user approval，2026-09-23）。Section 10 implementation may proceed on `OVERDRAWN` under TDD；`main` remains untouched.

## 10. Demo Prototype Execution After Approval

- [ ] 10.1 Scope cleanup: compare `OVERDRAWN` with `main` and revert every code、test、fixture、config、API／UI and Runner diff introduced solely for A8／A9 or changes to main A3S SG Redemption／Available Balance／Account Entries. Retain normalized A3S Covered／Excess calculation only. Do not modify `main`; prove existing main A3S behavior through baseline regression.
- [x] 10.2 Combined backend TDD: implement event invariants、A3S normalized calculation、A3／A3S Acknowledge split lock、pre-Acknowledge atomic Fix and A4／A6 locked-split finalisation. Prove A3／A3S LC UTILIZE及A4／A6 formal capacity impact只等於Covered，Excess不令Tight為負；A6 stores full Legal Acceptance plus locked attribution and A7 only reduces Legal Outstanding. Include `4000 + 6000 → 10000; Arrival 10200 → Covered 10000 / Excess 200`, pending-self-leg and rollback cases. — Applicant Waiver gate／snapshot已由10.2c明確取代；A4／A6使用共同ABSENT Checker Approve。A6 amount/currency lock and A7 Legal-only partial／full settlement integration are GREEN.
- [x] 10.2a BD-16 boundary TDD: LC face amount `10,000`、Tolerance `10%`、allowance `2%` uses Covered capacity `11,000` but Percentage Allowance `200`; prove Arrival `11,200` PASS and `11,201` returns `EXCESS_LIMIT_EXCEEDED` with zero writes.
- [x] 10.2b BD-16 renewed Change Approval: BA、4-Eyes、independent QA and `openspec validate --all --strict --no-interactive` PASS；business user approval received on 2026-09-23.
- [x] 10.2c SUPERSEDED by 2026-09-24 BUG decision: A4／A6 follow B4's ABSENT Checker approval path. Positive A3／A3S Excess no longer requires／persists Applicant Waiver and missing legacy waiver fields do not return `APPLICANT_WAIVER_REQUIRED`; shared Checker Approve remains the UI Release gate, while FX／allowance／eligibility and atomic reservation conversion remain authoritative.
- [x] 10.3 Combined export/accounting TDD: implement B4 authorization／4-Eyes gate、dedicated `EXPORT_EXCESS_ASSET` mapping and atomic Sight／Usance vouchers; prove B3 earmark及B4 capacity finalisation只等於Covered、Excess不重複扣減Tight、B3 does not pre-decide debtor、partial authorization routes the full Excess to Recourse, and B5 does not merge or auto-clear assets. — B4 create uses locked Legal／Covered／Excess, full authorization attributes Excess to Issuing Bank, partial／invalid／absent attributes the whole Excess to Recourse, Checker=Maker is zero-write, and Sight／Usance reconcile exactly.
- [x] 10.4 Minimal API／UI／Inquiry／Business Case Runner: expose locked Legal／Covered／Excess、A3S normalized evidence、B4 authorization and asset reconciliation; include Runner-only A02／B02 remediation after exact `EXCESS_LIMIT_EXCEEDED`／`FINITE` guidance. A4／A6／B4 warning SHALL use the selected source A3／B3 event's own This Exceed Amount: covered-only B01 hides it while positive-Excess B02 shows it, never the LC aggregate. A4／A6／B4共用一個SOLID presentation-only Checker Excess Review component及相同ABSENT Checker Approve操作。— event-specific A4／A6／B4 API／UI TDD and live regression included.
- [ ] 10.4a Bug-alignment TDD for A3／A3S／B3 four Protected Exceed fields: add zero-write preview API／UI contracts for Previous active outstanding Pending＋Approved（terminal allowance commitments excluded；Fix self excluded once）、This=`max(0, transaction-effective capacity)`、Total=Previous＋This、Maximum=`MIN(face excluding tolerance × allowancePercentage, configuredMaximumUsd compliant conversion to transaction currency)`；derive Import LC Amount only as Issue＋cumulative formal／Checker-approved Increase−cumulative formal／Checker-approved Decrease and prove Pending／unapproved amendment及Tolerance不影響它；prove Maximum is not the raw converted cap and either zero configuration yields Maximum `0`／no Excess with zero FX；cover the exact example other Pending `100`＋Approved `200`＋self `50`＋terminal `900`, replacement `10,500` against capacity `10,000`, yielding Previous `300`／This `500`／Total=Maximum `800`, plus `10,500.01` over-limit zero-write；also cover USD_PAR zero-call、non-USD provider BOOKING Approved／Effective／Fresh、typed unavailable／stale fail-closed and authoritative Maker Submit recalculation that never trusts preview input.
- [x] 10.4b UI lifecycle cleanup: B4 Release回到Index後清除completed Authorization／Export Asset Inquiry；A3／A3S／B3僅在Previous或This Exceed為正數時顯示Excess Preview與Maker Result Excess breakdown，零超押仍執行authoritative preview validation但不顯示多餘明細。— TDD focused 3 suites／587 tests PASS；live B4 reset regression included.
- [x] 10.4c Unified Checker Excess UI: A4／A6／B4使用完全相同的紅色This Exceed＋Checker Approve presentation；Release未勾選時disabled，Reject仍可用；B4 demo UI固定使用ABSENT authorization path並保留backend API／data-model contract。— 2026-09-24 component／parent TDD、A4／A6 backend zero-waiver-snapshot、Runner ABSENT paths及live UI regression PASS；BA、4-Eyes、QA均PASS。
- [ ] 10.5 Quality gate: run focused TDD、main A3S baseline、full regression and reproducible ARM64 SonarQube with fresh LCOV against approved thresholds; update traceability evidence without duplicate narrative documents. — 2026-09-24：Angular 71 suites／2,075 tests、backend 3／81、microservice 68／1,293 assertions全PASS；ARM64 Sonar實掃整體Coverage 92.2%、新Duplication 0%、Security Issues 0，但Quality Gate仍FAIL（New Coverage 84.8%、8 New Issues及既有Overall Issues thresholds），故不得勾選完成或封版。
- [ ] 10.6 Closure: obtain final BA、4-Eyes and independent QA PASS, rerun strict validation, obtain closure approval and only then archive the Change.

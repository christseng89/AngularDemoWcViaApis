{
  "name": "balance-component-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Node.js 中台 orchestrator — sequences calls into the balance-component microservice per a declarative Business Case Registry (Import Case 1-5, Export Case 1-5) so the Angular UI can run/replay a whole scenario with one click.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "lint": "eslint server.js data test",
    "format:check": "prettier --check \"*.js\" \"data/**/*.js\" \"test/**/*.js\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.2",
    "helmet": "^8.3.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "jest": "^29.7.0",
    "prettier": "^3.9.6",
    "supertest": "^6.3.4"
  }
}

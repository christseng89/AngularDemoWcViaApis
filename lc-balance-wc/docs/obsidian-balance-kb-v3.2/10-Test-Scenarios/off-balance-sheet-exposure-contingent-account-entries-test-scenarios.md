---
knowledge_id: off-balance-sheet-exposure-contingent-account-entries-test-scenarios
title: "表外风险敞口与或有科目分录 测试场景"
domain: Balance
category: Test Scenarios
snapshot_date: 2026-08-22
tags:
  - balance
  - test-scenario
---

# 表外风险敞口与或有科目分录 测试场景

从本主题范围的测试文件中提取了19个测试场景。这些场景所证明的规则详见 [[Off-Balance-Sheet Exposure|表外风险敞口与或有科目分录]] 与 [[Business-Rule-Index]]。

| 场景 | 前置条件（Given） | 触发操作（When） | 预期结果（Then） | 来源 |
|---|---|---|---|---|
| SG 表外风险敞口会将未结清的 ISSUE 与已 RELEASED 的赎回以及仍处于 PENDING 状态的 ISSUE 相互抵销 | SHGT 动账：ISSUE 100000 RELEASED，PARTIAL_REDEEM 30000 RELEASED，ISSUE 10000 PENDING | 在未设置 matched-businessEventId 的情况下调用 computeOffBalanceExposure() | 返回 80000（100000 − 30000 + 10000）——PENDING 状态的 ISSUE 会立即占用容量——*余额影响：* LC 的 Tight Available Balance 会因这 80000 已预留但尚未提用的 SG 风险敞口而减少 *容差/汇率：* 不适用——表外风险敞口使用的是 ceilingAmount，上游已完成容差换算 | `test/unit/domain/offBalanceExposure.test.ts:18-25` |
| FULL_REDEEM 会将 ISSUE 完全抵销至零风险敞口 | SHGT 动账：ISSUE 50000 RELEASED，FULL_REDEEM 50000 RELEASED | 调用 computeOffBalanceExposure() | 返回 0——*余额影响：* SG 已完全解付——对父级 LC 的 Tight Available Balance 不再有任何拖累 *容差/汇率：* 不适用 | `test/unit/domain/offBalanceExposure.test.ts:27-33` |
| CANCELLED/REJECTED/SUPERSEDED 状态的 SHGT 动账永远不会计入风险敞口 | SHGT 动账：ISSUE 100000 RELEASED，ISSUE 999999 CANCELLED | 调用 computeOffBalanceExposure() | 返回 100000——被 CANCELLED 的 999999 完全被忽略——*余额影响：* 被取消的 SG Issue 从不预留任何容量 *容差/汇率：* 不适用 | `test/unit/domain/offBalanceExposure.test.ts:35-41` |
| checkUtilizeSufficiency 会拒绝在 Available Balance 内、但超出 Tight Available Balance 的金额（v0.12 硬性 ERROR） | availableBalance=121000，confirmedBalance=121000，pendingDecreaseTotal=0，offBalanceExposure=100000 | 调用 checkUtilizeSufficiency({requestedAmount: 50000, ...}) | ok:false，报错中提及 "exceeds Tight Available Balance 21000"，并指向 "Document Arrival w/ Shipping Gtee" 作为替代方案——*余额影响：* 即便 50000 < 121000 的普通 Available，Document Arrival 仍被拒绝，因为超出了 21000 的 Tight Available（121000 − 100000 的 SG 风险敞口）*容差/汇率：* 不适用 | `test/unit/domain/offBalanceExposure.test.ts:94-106` |
| 无论风险敞口如何，checkUtilizeSufficiency 都会拒绝超出普通 Available Balance 的金额 | availableBalance=121000，confirmedBalance=121000，offBalanceExposure=0 | 调用 checkUtilizeSufficiency({requestedAmount: 200000, ...}) | ok:false，报错中提及 "exceeds Available Balance"——*余额影响：* 第一层校验会先触发，甚至不会到达 tight 阈值校验 *容差/汇率：* 不适用 | `test/unit/domain/offBalanceExposure.test.ts:108-119` |
| checkUtilizeSufficiency 边界情况：恰好等于 Tight 阈值时通过 | availableBalance=121000，confirmedBalance=121000，offBalanceExposure=100000 → tightAvailable=21000 | 调用 checkUtilizeSufficiency({requestedAmount: 21000, ...}) | ok:true——*余额影响：* 恰好达到边界的金额是被允许的（采用严格大于比较，而非 ≥）*容差/汇率：* 不适用 | `test/unit/domain/offBalanceExposure.test.ts:121-130` |
| A3S 匹配抵销：完全匹配的 SG 赎回会在 UTILIZE 校验运行之前，将 offBalanceExposure 降为 0 | offBalanceExposure=0（调用方已提前完成抵销，因为匹配的 SG 自身的 PENDING 状态 FULL_REDEEM 在本次调用之前已被创建） | 调用 checkUtilizeSufficiency({requestedAmount: 100000, availableBalance: 121000, confirmedBalance: 121000, ...}) | ok:true——*余额影响：* 匹配的 SG 所预留的 100,000 容量被视为已释放，因此完整的 Document Arrival 金额得以通过 *容差/汇率：* 不适用 | `test/unit/domain/offBalanceExposure.test.ts:132-144` |
| 仍处于 PENDING 状态的 ISSUE 会拉高 Available 但不会拉高 Confirmed——在 Approval 之前，Tight Available Balance 会阻止任何提用 | 一笔全新的 A1 ISSUE：availableBalance=10（包含该 PENDING 状态的 ISSUE），confirmedBalance=0（尚未 Released） | 调用 checkUtilizeSufficiency({requestedAmount: 10, ...}) | ok:false，报错中提及 "exceeds Tight Available Balance 0"——*余额影响：* "A1/A2 Submit -> A4 不可使用, APPROVE後才可以動用"——在 Checker 批准该 ISSUE 之前，容量不可使用 *容差/汇率：* 不适用 | `test/unit/domain/offBalanceExposure.test.ts:146-157` |
| 仍处于 PENDING 状态的 AMEND_DECREASE 会立即占用 Tight Available Balance，而另一笔独立的 PENDING 状态增加则会被正确忽略 | confirmedBalance=100000，availableBalance=130000（100000 + 50000 的待处理增加 − 20000 的待处理减少），pendingDecreaseTotal=20000 | 调用 checkUtilizeSufficiency({requestedAmount: 80001, ...}) | ok:false，报错中提及 "exceeds Tight Available Balance 80000" 与 "Confirmed Balance 100000 minus 20000 still-PENDING decrease"——*余额影响：* Tight Available = 100000 − 20000 = 80000，绝不会因那 50000 仍处于 PENDING 状态的增加而被拉高 *容差/汇率：* 不适用 | `test/unit/domain/offBalanceExposure.test.ts:159-171` |
| 一旦第一笔 SG Issue 已被计入既有风险敞口，checkShgtIssueSufficiency 就会拒绝第二笔重叠的 SG Issue | parentConfirmedBalance=100000，existingShgtExposure=90000（第一笔 SG 已处于未结清状态） | 调用 checkShgtIssueSufficiency({requestedAmount: 90000, ...})（针对同一份 LC 的第二笔 SG） | ok:false，报错中提及 "exceeds parent LC's Tight Available Balance 10000"——*余额影响：* 防止两笔重叠的 SG 开立在各自单独校验时都能分别通过 *容差/汇率：* 不适用 | `test/unit/domain/offBalanceExposure.test.ts:213-223` |
| checkPresentDocsIssueSufficiency 复现并拒绝了 E01+E02+E03 总和从未被校验的漏洞 | parentConfirmedBalance=100000，presentDocsEarmark=120000（E01 50k + E02 70k 已处于未结清状态） | 针对 E03 调用 checkPresentDocsIssueSufficiency({requestedAmount: 100000, ...}) | ok:false，报错中提及 "Present Docs amount 100000 exceeds the parent Confirmation's Present Earmark-adjusted Tight Available Balance -20000" 以及 balanceContractId——*余额影响：* 补上了历史上的漏洞——此前 E01/E02/E03 各自单独校验时，都能分别通过针对同一个仍显示满额的 Available Balance 的校验 *容差/汇率：* 不适用 | `test/unit/domain/offBalanceExposure.test.ts:272-284` |
| computePresentDocsEarmark 的综合桶会完全剔除已被 B4 消耗的提示单据 | e1 CREATE 50000 PENDING，e2 CREATE 70000 RELEASED（未消耗），e3 CREATE 999999 RELEASED 且已设置 presentDocsConsumedAt | 调用 computePresentDocsEarmark(exam) | 返回 120000（50000 + 70000）——e3 因已被 B4 消耗而被排除——*余额影响：* 已完成（经 B4 Honour/Accept）的提示单据不再占用任何 earmark 容量 *容差/汇率：* 不适用 | `test/unit/domain/offBalanceExposure.test.ts:50-57` |
| Present Docs Earmark 的 Pending/Approved 拆分各自独立汇总 | e1 CREATE 50000 PENDING，e2 CREATE 70000 RELEASED（未消耗） | 同时调用 computePresentDocsEarmarkPending(exam) 与 computePresentDocsEarmarkApproved(exam) | Pending 返回 50000；Approved 返回 70000——*余额影响：* 与综合的 120000 earmark 相符，按 Checker 是否已真正 Release 该提示单据来拆分 *容差/汇率：* 不适用 | `test/unit/domain/offBalanceExposure.test.ts:59-74` |
| IPLC_LC 的 Sight ISSUE 会生成带 tenor 后缀的建立分录对（Folio 1） | instrumentType=IPLC_LC，movementType=ISSUE，tenorType=SIGHT，amount=50000，currency=USD | 调用 deriveContingentAccountEntry() | 返回 {drAccount: "Customers' Liability under DC — Sight", crAccount: "Documentary Credits Outstanding — Sight", currency: USD, amount: 50000}——*余额影响：* 新的 LC Issue 建立或有风险敞口——借记客户负债侧，贷记未结清侧 *容差/汇率：* 这里的 amount 是传入本函数的原始动账金额（由调用方按需提供 ceilingAmount/amount）——本模块内部不会独立进行容差换算 | `test/unit/domain/contingentAccountEntry.test.ts:6-17` |
| IPLC_LC 的 UTILIZE 会翻转为释放方向的分录对（相对 ISSUE，借贷方向互换） | instrumentType=IPLC_LC，movementType=UTILIZE，tenorType=SIGHT，amount=50000 | 调用 deriveContingentAccountEntry() | 返回 {drAccount: "Documentary Credits Outstanding — Sight", crAccount: "Customers' Liability under DC — Sight"}——恰好与 ISSUE 的分录对相反——*余额影响：* Document Arrival/UTILIZE 会释放（减少）或有 LC 风险敞口 *容差/汇率：* 不适用 | `test/unit/domain/contingentAccountEntry.test.ts:6-17` |
| SHGT 的科目对完全忽略 tenorType | SHGT ISSUE 分别以 tenorType=SIGHT 与相同调用但 tenorType=SELLERS_USANCE 进行对比 | 两种情况下均调用 deriveContingentAccountEntry() | 两次调用返回完全相同的分录——SG_FAMILY.tenorSuffix 为 'NONE'——*余额影响：* SG 自身的或有分录对绝不会因父级 LC 所声明的 tenor 而变化 *容差/汇率：* 不适用 | `test/unit/domain/contingentAccountEntry.test.ts:52-56` |
| 带负数金额的 EPLC_CONFIRMATION AMEND 会翻转为释放方向的分录对，并以绝对值报告金额 | instrumentType=EPLC_CONFIRMATION，movementType=AMEND，amount=-10000，tenorType=SIGHT | 调用 deriveContingentAccountEntry() | 返回 {drAccount: "Confirmation Undertakings Outstanding — Sight", crAccount: "Issuing Bank Confirmation Exposure — Sight", amount: '10000'}——输入的符号会翻转 netDirection，输出的金额始终为非负——*余额影响：* B2 Amendment Decrease 会过账释放方向的分录对，即便 B2 并没有独立的 AMEND_DECREASE movementType——方向完全依附于金额自身的符号 *容差/汇率：* 无论币种为何，该分录的金额始终以绝对值量级输出（例如 -2500.50 → '2500.5'），绝不会是带符号的值 | `test/unit/domain/contingentAccountEntry.test.ts:128-153` |
| EPLC_EXAMINATION（B3 Present Docs）不会生成任何科目分录 | instrumentType=EPLC_EXAMINATION，movementType=CREATE，amount=5000 | 调用 deriveContingentAccountEntry() | 返回 null——*余额影响：* B3 的提示单据永远不会创建任何借贷分录——它仅为 MEMO_ONLY，从不过账到账簿 *容差/汇率：* 不适用 | `test/unit/domain/contingentAccountEntry.test.ts:168-171` |
| 无论 movementType 为何，表内资产类 instrumentType 始终返回 null | instrumentType ∈ {EPLC_DUE_FROM_ISSUING_BANK, EPLC_ACCEPTANCE_REIMB_RECEIVABLE, EPLC_EXPORT_BILLS_DISCOUNTED}，分别以 CREATE 与 REIMBURSE 进行测试 | 调用 deriveContingentAccountEntry() | 所有情况均返回 null——*余额影响：* 这些资产侧的单证类型被明确排除在 Balance Component 自身的或有/表外范畴之外 *容差/汇率：* 不适用 | `test/unit/domain/contingentAccountEntry.test.ts:174-182` |
